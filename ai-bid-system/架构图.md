[用户上传标书 .docx]
        │
        ▼
===================================================================================
【第一阶段：底层解析与分流网关 (Parser & Router)】 -> 建议逐步移交至 Python 后端处理
===================================================================================
        │
        ├─ 扫描普通段落 <w:p> ───────────────> 提取普通下划线/括号
        │
        └─ 扫描完整表格 <w:tbl> ─────────────> 提取 [表头指纹] + [上方上下文锚点]
                  │
                  ▼
          (智能表格分类器 Table Classifier)
                  │
                  ├─ 匹配到【简历/业绩】指纹 ──> 标记为 `dynamic_table` (动态扩展表)
                  ├─ 匹配到【偏离/审查】指纹 ──> 标记为 `manual_table` (人工核对表)
                  ├─ 匹配到【报价/清单】指纹 ──> 标记为 `pricing_table` (禁止AI触碰)
                  └─ 无明显指纹 (普通小网格) ──> 降维打碎，混入普通空白


===================================================================================
【第二阶段：物理隔离的三条生产线 (Isolated Processing Lanes)】
===================================================================================
        │
        ├─ [普通空白流] (normal_blanks)
        │       └─> 发送至现有的 Dify 填空工作流 -> 输出零散的填空字符串
        │
        ├─ [动态表格流] (dynamic_tables) 
        │       └─> 发送至全新的 Dify JSON组装工作流 -> 提取人员库数据 -> 输出标准JSON数组
        │
        └─ [高危表格流] (manual/pricing_tables)
                └─> 挂起，不调用 AI，直接流转到前端人工干预区


===================================================================================
【第三阶段：前端人机协作中枢 (Human-in-the-Loop UI)】 -> CreateBid.jsx
===================================================================================
        │
        ├─ [普通填空视图] ──> 现有的 List/Table，用户浏览 AI 填入的散文本。
        │
        ├─ [动态表格视图] ──> 新增！专门展示简历表/业绩表，用户下拉选人，支持增删改查 AI 生成的 JSON 行。
        │
        └─ [高危警报视图] ──> 新增！高亮提示“检测到报价/偏离表，为防风险请导出后人工填写”。


===================================================================================
【第四阶段：底层原格式融合 (XML DOM Injector)】 -> merge_server.py
===================================================================================
        │ (用户点击导出，前端发送完整 JSON payload)
        ▼
  (Python lxml 解析器接管)
        │
        ├─ 注入普通空白 ──> 坐标寻址，直接替换文本节点。
        │
        └─ 注入动态表格 ──> 寻址 <w:tbl> -> 提取模板行 <w:tr> -> 内存中循环克隆(deepcopy) 
                            -> 将 JSON 数据逐列塞入 -> 挂载新行 -> 删除原模板行
        │
        ▼
[完美保留原格式的已填报 .docx 下载]